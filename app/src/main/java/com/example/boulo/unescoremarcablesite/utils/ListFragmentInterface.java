package com.example.boulo.unescoremarcablesite.utils;

public interface ListFragmentInterface {
    //interface used when the user click on an item of the recycler view
    void showSiteDetails(String siteName);
}
